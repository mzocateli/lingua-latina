{
  "name": "lingua-latina",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "validate": "node scripts/validate.mjs",
    "materia": "node scripts/extract-material.mjs && node scripts/lexicon.mjs --build && node scripts/locus.mjs --build",
    "locus": "node scripts/locus.mjs"
  },
  "devDependencies": {
    "astro": "^4.16.0",
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0"
  }
}
